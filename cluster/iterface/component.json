{
  "name": "core.cluster.interface",
  "type": "c++",

  "deps": [
    "core.cluster.node",
    "core.compile-time",
    "core.context.static",
    "core.exceptions",
    "core.interface.metadata.store"
  ],

  "targets": {
    "debug":   { "type": "lib" },
    "release": { "type": "lib" },
    "test": {
      "deps": [
        "core.testing.metadata",
        "core.testing.static"
      ],
      "type": "lib"
    }
  }
}
