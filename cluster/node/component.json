{
  "name": "core.cluster.node",
  "type": "c++",

  "deps": [
    "core.compile-time",
    "core.context.static",
    "core.utility.status"
  ],

  "targets": {
    "debug":   { "type": "lib" },
    "release": { "type": "lib" },
    "test": {
      "deps": [
        "core.model.cli-parser"
      ],
      "type": "lib"
    }
  }
}
